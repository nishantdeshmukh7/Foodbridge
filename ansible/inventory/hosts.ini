# Example inventory. Replace the placeholder host below with your real
# target(s) - a fresh Ubuntu 22.04 VM/EC2 instance that will run this
# repo's Docker images (see docker-compose.prod.yml). "app_servers" is the
# only group the playbook (playbooks/site.yml) targets.
#
# For local testing against a container instead of a real VM, see
# ../../docs/ANSIBLE.md's "How this was actually tested" section - that
# uses a separate, generated inventory, not this file.

[app_servers]
# foodbridge-app-01 ansible_host=203.0.113.10 ansible_user=ubuntu

[app_servers:vars]
ansible_python_interpreter=/usr/bin/python3
