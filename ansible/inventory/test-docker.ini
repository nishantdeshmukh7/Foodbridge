# NOT part of the real inventory - used only to locally verify roles/site.yml
# actually work and are idempotent, against a real systemd-capable Ubuntu
# container (see docs/ANSIBLE.md's "How this was actually tested" section).
# Never referenced by ansible.cfg's default inventory path.
[app_servers]
ansible-test-host ansible_connection=docker

[app_servers:vars]
ansible_python_interpreter=/usr/bin/python3
